# Public, non-secret 0G deployment metadata.
NEXT_PUBLIC_AGENTSHIELD_REGISTRY=

# Server-only 0G Compute settings. Never prefix these with NEXT_PUBLIC_.
# A service URL is required in addition to the provider-issued API secret.
ZG_SERVICE_URL=
ZG_API_SECRET=
ZG_MODEL=
